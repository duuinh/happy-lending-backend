export enum ItemStatusEnum {
    available = 'available',
    notAvailable = 'not_available',
    unlisted = 'unlisted',
}

export enum ContractStatusEnum {
    created = 'created',
    accepted = 'accepted',
    rejected = 'rejected',
    closed = 'closed',
}