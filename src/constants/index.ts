export enum ItemStatusEnum {
    available = 'available',
    notAvailable = 'not_available',
    unlisted = 'unlisted',
}

export enum ContractStatusEnum {
    requested = 'requested',
    accepted = 'accepted',
    rejected = 'rejected',
    closed = 'closed',
}